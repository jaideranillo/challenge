package com.cobre.challenge.domain.model.subscription.enums;

/**
 * Target-URL ownership handshake state on a {@link Subscription} (ADR-005 SS3).
 */
public enum VerificationState {
    PENDING_VERIFICATION,
    VERIFIED
}
